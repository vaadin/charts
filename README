This is parent project that builds Vaadin Directory compatible zip that contains both v7 and v6 compatible version of add-ons. Normally, during development, developers should work with addon or addon6 (standalone) modules.

demo builds a web app that shows most of our integration tests in a simple demo app.

chart-export-demo demonstrates how charts can be converted to SVG and via it used in e.g. PDF documents.

chart-plugin-demo shows how the underlaying Highcharts library version can be changed and how "Highcharts plugins" can be used with Vaadin Charts.
